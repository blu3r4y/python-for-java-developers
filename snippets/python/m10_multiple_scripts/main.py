test_variable = 42

# the Python interpreter will set the global variable __name__ for us,
# so that we can check it here and create a so-called "main function"
if __name__ == "__main__":
    print("This will only run if you specifically started the interpretor on this file.")
